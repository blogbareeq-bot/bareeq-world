# Bareeq V4.21.2 — Merge Report

التاريخ: 2026-08-21 (UTC)

الحزمة: `Bareeq-V4.21.2-FINAL.zip`

الفرع: `arena/01a02617-bareeq-world`

الأساس المستقر: V4.21.1 `6a98911628765568ecc540f47f10bc6c288ffeff`

## القرار

نُفّذ دمج انتقائي يحافظ على إضافات كلود المفيدة، ويقفل **التصميم الأول** للهيدر، ويصلح حركة **شريط أحدث المقالات** على الجوال. أُزيلت فقط الطبقات التي تزاحم تجربة قراءة المقال أو تعيد تقديم محتواه بلا حاجة.

## إضافات كلود المحفوظة

- المقدمة المختصرة في الرئيسية وصياغة «قراءات تحريرية» في صفحة «عن بريق».
- مقال توقيع لكل قسم، وترتيب السلاسل ووعدها التحريري في صفحات الأقسام والسلاسل و«ابدأ من هنا».
- مقتطفات البحث المستخرجة من المحتوى، مع إبقاء إصابة كلمات مثل «مدار».
- `noindex` لصفحة المحفوظات وإزالة تداخل عنصر `<main>` فيها.
- تصحيح ثلاث خلاصات سريعة، وتوحيد وسم `الذكاء الاصطناعي` مع تحويل 301 من الوسم القديم.
- بوابات التحقق الخاصة بالإصدار 4.21.2 وتحسينات التقارير.

## ما نُقّح

- حُذفت `bareeqAddition` من مخطط المحتوى والمقالات؛ لأنها تكرر طبقة إضافية داخل تجربة القراءة.
- حُذفت بطاقة «أكمل المسار» من نهاية المقال، مع إبقاء السلاسل وصفحاتها وترتيبها خارج متن المقال.
- استُبدلت أيقونة المحفوظات التعبيرية بأيقونة SVG ثابتة حتى لا تظهر كمربع فارغ على بعض الأنظمة.
- حُذف مولّد اللقطات الاصطناعية، واستُبدلت ملفاته بلقطات PNG حقيقية من Chromium.
- نُظّفت مجلدات استعادة الصوت المؤقتة تلقائيًا، وأصبحت بوابة التوزيع ترفض تسربها إلى `dist`.

## إصلاح شريط المقالات

- مجموعتان متطابقتان من العناوين داخل مسار واحد؛ الثانية `aria-hidden="true"` لضمان حلقة سلسة بلا تكرار لقارئ الشاشة.
- حركة JavaScript مقاسة بعرض المجموعة الأصلية على الجوال حتى 700px، وحركة CSS على الأجهزة الأكبر.
- زر الإيقاف يوقف الحركة فعلًا، و`prefers-reduced-motion` يعطّل التحريك التلقائي ويبقي التمرير اليدوي.
- إعادة قياس بعد تغيّر المقاس أو الاتجاه أو تحميل الخطوط، وإيقاف الإطارات حين تكون الصفحة غير مرئية.
- لا يوجد تمرير أفقي للصفحة، ولا اعتراض لإيماءات النقر على روابط المقالات.

## ما بقي ثابتًا

- 13 مقالًا منشورًا، بلا تغيير في متونها.
- عنوان H1 للرئيسية: `عالم بريق — نافذتك إلى المعرفة`.
- التصميم الأول فقط؛ لا أثر للنموذجين الثاني والثالث.
- بوابة Cloud TTS المدفوعة غير مفعّلة، ولم يُجرَ أي طلب TTS/API خارجي أثناء التجهيز.

## نتيجة التحقق

- `npm run build`: ناجح، 56 صفحة و56 ملف HTML.
- الصوت: 13/13 مقالًا، 61 جزءًا متزامنًا و61 ملف MP3 متحققًا منه.
- Chromium 140: ناجح عند 320 / 360 / 390 / 430 / 768 / 1024 / 1440 بكسل.
- حركة الجوال والإيقاف وتقليل الحركة: ناجحة، بلا أخطاء console/page وبلا overflow أفقي.
- فحوص الاستجابة والتباين والتفاعل والتوزيع: ناجحة.

## الحالة التشغيلية

المرشح المحلي جاهز. لم يُدفع إلى GitHub ولم يُدمج في `main` بعد؛ ويتطلب فحص تحويل الوسم القديم على Cloudflare نشر الفرع أولًا.
