name: Bareeq coworker ASR correction (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after coworker exact dual-ASR closure; no automatic provider calls remain here.'
