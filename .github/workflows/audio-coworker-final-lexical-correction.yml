name: Bareeq coworker final lexical correction (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after exact coworker 0/0/0/0. Final validation is handled by audio-openrouter-dual-asr-final.yml.'
