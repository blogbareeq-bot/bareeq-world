name: Verify regenerated Gemini pilot with 2.5

on:
  push:
    branches:
      - audio/fahed-v4220
    paths:
      - .github/workflows/verify-regenerated-gemini-pilot-25.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: regenerated-gemini-pilot-25-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout exact regenerated branch state
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Validate exact source audio and no-text-leak contract
        run: |
          node --check scripts/verify-gemini-pilot-transcript-chunked.mjs
          node scripts/test-arabic-transcript-match.mjs
          node scripts/test-gemini-audio-transcript-contract.mjs
          node - <<'NODE'
          const fs = require('node:fs');
          const crypto = require('node:crypto');
          const meta = JSON.parse(fs.readFileSync('scripts/speech-test-evidence/how-touchscreens-work-gemini-pilot-v1.json', 'utf8'));
          const audio = fs.readFileSync('scripts/speech-test-evidence/how-touchscreens-work-gemini-pilot-v1.mp3');
          const sha = crypto.createHash('sha256').update(audio).digest('hex');
          if (sha !== 'e04466b4824db92821488b1fd2ee848e3312fded968a89ff91fdb97f1b15550e') throw new Error(`Unexpected regenerated MP3 SHA: ${sha}`);
          if (meta.sha256 !== sha || meta.bytes !== audio.length) throw new Error('Regenerated evidence integrity mismatch.');
          console.log(`Independent ASR target locked to regenerated SHA ${sha}.`);
          NODE

      - name: Require encrypted Gemini credential
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: test -n "$GEMINI_API_KEY"

      - name: Build independent Gemini 2.5 verifier
        run: |
          cp scripts/verify-gemini-pilot-transcript-chunked.mjs scripts/.verify-regenerated-25.mjs
          sed -i "s/const MODEL = 'gemini-3.7-flash';/const MODEL = 'gemini-2.5-flash';/" scripts/.verify-regenerated-25.mjs
          grep -F "const MODEL = 'gemini-2.5-flash';" scripts/.verify-regenerated-25.mjs
          node --check scripts/.verify-regenerated-25.mjs

      - name: Require two independent exact zero-error ASR passes
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          BAREEQ_ASR_MIN_INTERVAL_MS: "6000"
          BAREEQ_ASR_CHUNK_IDEAL_SECONDS: "24"
          BAREEQ_ASR_CHUNK_MAX_SECONDS: "32"
          BAREEQ_ASR_CHUNK_HARD_MAX_SECONDS: "42"
        run: node scripts/.verify-regenerated-25.mjs

      - name: Remove temporary verifier
        if: always()
        run: rm -f scripts/.verify-regenerated-25.mjs

      - name: Preserve independent verification report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regenerated-gemini-pilot-25-report-${{ github.run_id }}
          path: scripts/speech-transcript-evidence/how-touchscreens-work-gemini-pilot-v1.json
          if-no-files-found: ignore
          retention-days: 14

      - name: Commit independent zero-error evidence only
        if: success()
        run: |
          git config user.name 'bareeq-audio-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add scripts/speech-test-evidence/how-touchscreens-work-gemini-pilot-v1.json scripts/speech-transcript-evidence/how-touchscreens-work-gemini-pilot-v1.json
          git diff --cached --quiet && { echo 'Independent evidence already current.'; exit 0; }
          git commit -m 'audio: record Gemini 2.5 zero-error ASR for regenerated pilot'
          git pull --rebase origin ${{ github.ref_name }}
          git push origin HEAD:${{ github.ref_name }}

      - name: Keep human listening gate closed
        if: success()
        run: |
          node - <<'NODE'
          const plan = require('./scripts/speech-test-clips/how-touchscreens-work.json');
          if (plan.testClipPassed || plan.fullSynthesisAllowed || plan.audioReview?.status === 'passed') {
            throw new Error('Independent automated ASR must not impersonate human listening approval.');
          }
          console.log('Independent automated ASR passed; human listening gate is still separate.');
          NODE
