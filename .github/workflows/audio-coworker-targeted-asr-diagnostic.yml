name: Bareeq coworker targeted ASR diagnostic (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after coworker exact dual-ASR closure to protect provider quota.'
