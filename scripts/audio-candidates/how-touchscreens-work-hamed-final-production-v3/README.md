# Quarantined candidate: how-touchscreens-work — Hamed final production v3

**Status: DO NOT PUBLISH.** Moved out of `public/audio/articles/de93f3d9f91c8b8b/`
before it could ship, because it has not passed the mandatory full-article QA gate.

## What this is

- Article: كيف تعمل شاشات اللمس (how-touchscreens-work), audio key `de93f3d9f91c8b8b`.
- Voice: Microsoft `ar-KW-HamedNeural` (substitute path; user's primary decision
  remains Arabic (Kuwait) – Fahed, rejected by listening on 2026-08-25).
- 10 parts `hamed-part-001..010-f4a18ef4.mp3`, speech sourceHash `f4a18ef4ee13…`
  (10-segment split of the same script, rev1 `dd0e74ecdb7f`).
- Manifest version 3, generatorVersion 8 (built by the parallel
  `audio/fahed-v4220` session; commit `206ee4f` lineage).

## Why it is quarantined

1. **Stable-9 evidence FAILED**: the other session's own committed evidence
   (`scripts/speech-test-evidence/…stable9-test…`) has `status: "failed"` —
   the 9 stable ASR-disagreement words from the corrected-full-v2 run were
   retested and did not come back clean.
2. **Full two-model QA run FAILED**: Actions run `32948126643`
   (`qa-hamed-final-production-v3.yml`) failed after ~10m — the full-article
   word-level ASR gate did not pass.
3. The only passing artifact is the final3 adjudication
   (`final3-gemini35`, 58-word target segment, PASSED). A single-segment
   adjudication does not satisfy the zero-deletion/insertion/substitution
   gate over the whole ~1507-word article.

Per the factory rules: when a doubtful position cannot be proven sound,
the article is not published.

## Rollback / live state

- Live published audio remains the legacy 3-part bundle
  (`hamed-part-001..003-53830592.mp3`, sourceHash `538305924f58…`), restored
  to `public/audio/articles/de93f3d9f91c8b8b/`.
- The Gemini Sadaltager regeneration for this article is resumable from
  checkpoint `scripts/audio-checkpoints/how-touchscreens-work-sadaltager/part-001`
  (~156.6 s) — this remains the preferred completion path (single model,
  matches the two retained live Gemini articles).

## Files here

- `manifest.json` — candidate manifest as generated by the v3 pipeline.
- `hamed-part-001..010-f4a18ef4.mp3` — candidate audio parts.
- Related evidence stays in `scripts/speech-test-evidence/` and
  `scripts/speech-transcript-evidence/` (final3 passed, stable9 failed).
