# تقرير اختبار Bareeq V4.16.1 — Cedar + Hamed Zero-Cost Release

**التاريخ:** 15 أغسطس 2026  
**البيئة:** Node.js 24.19.0، npm 11.9.0، FFmpeg/ffprobe 6.1.1  
**النطاق:** تثبيت الصوت المنشور، سلامة الملفات، مزامنة النص، الاستئناف 30 يومًا، البناء، الجوال والتابلت، SEO والأمان، والحفاظ على جميع إصلاحات الإصدارات السابقة.

## الحكم

**PASS — مصدر V4.16.1 وبناء الإنتاج المجاني اجتازا جميع البوابات.**

نُفذ `npm ci` من `package-lock.json` المحدث وثُبتت 250 حزمة، ثم أعاد `npm audit`:

```text
info: 0, low: 0, moderate: 0, high: 0, critical: 0, total: 0
```

ثُبت `nanoid` بالإصدار المصحح `3.3.18` بدل `3.3.17` الذي ظهر في سجل Cloudflare.

## تكوين الإنتاج المعتمد

| البند | النتيجة |
|---|---:|
| المقالات المنشورة | 11 |
| Studio Cedar الحقيقي | مقال واحد، ملف إنتاج متصل |
| Azure Hamed المثبت | 10 مقالات، 29 MP3 |
| إجمالي MP3 في `dist` | 30 |
| طلبات التوليد الافتراضية | 0 |
| الأحرف المدفوعة الافتراضية | 0 |
| مفاتيح Cloudflare المطلوبة | 0 |
| مدة Cedar | 281.088 ثانية = 4:41 |
| حفظ موضع الاستماع | 30 يومًا بالضبط |

خطة البناء الافتراضية الفعلية:

```text
Bundled mixed audio plan: 11 articles, 0 synthesis request(s), 0 billable character(s).
```

لا يعتمد الاستيراد على كاش الموقع الحي، ولا يتصل بـOpenAI أو Azure، ولا يقرأ مفتاحًا.

## بوابات السلامة

- قفل SHA-256 والحجم والمدة لكل ملف Azure Hamed.
- مطابقة معرف المقال والعنوان والصوت والمزود والصيغة.
- قفل نص وترتيب كتل كل مقال ببصمة مستقلة.
- مطابقة تلميح كل كتلة مزامنة مع النص المرئي الحالي.
- استيراد بلا اتصال شبكة أو API وباستبدال ذري قابل للاسترداد.
- منع نشر manifest ناقص أو ملف مفقود أو مسار هارب أو مفتاح سري.
- إبقاء Cedar Studio مقفلًا ببصمات المصدر والصوت والمدة والإصدار.

## نتائج الاختبارات

- تثبيت نظيف عبر `npm ci`: **PASS** — 250 حزمة.
- `npm audit`: **PASS** — صفر ثغرات بجميع الدرجات.
- خطة إنتاج `bundled`: **PASS** — 11 مقالًا، 0 طلب، 0 حرف مدفوع.
- استيراد Azure Hamed: **PASS** — 10 مقالات، 29 ملفًا، 93,092,832 بايتًا مقفلًا.
- استيراد Studio Cedar: **PASS** — ملف إنتاج متصل، 281.088 ثانية، 21 موضع مزامنة، مع فحص 21 جزء المصدر.
- بناء Astro كامل: **PASS** — 50 صفحة.
- ناتج الصوت: **PASS** — 30 MP3 حقيقيًا، 96,466,508 بايتات، 30 مسارًا، و772 كتلة مزامنة إنتاجية.
- الروابط والصور والوصول وSEO وCSP والاستجابة: **PASS**.
- التباين: **PASS** — 22 تركيبة فاتحة/داكنة عند WCAG AA.
- التفاعلات: **PASS** — Cedar وحامد، التقديم، الإيقاف، التتبع، الاستئناف وانتهاء 30 يومًا، القوائم، البحث والتصفية.
- Arabic Speech QA: **PASS** — 11/11 مقالًا و48 قاعدة نطق سياقية.
- اختبار عقد OpenAI المحلي غير المدفوع: **PASS** — 72 طلبًا محاكيًا موثقًا، 36 Cedar + 36 Marin، و73 MP3 مفحوصًا في وضع الترقية.

## حفظ موضع الاستماع 30 يومًا

اختُبر قبول آخر موضع داخل المدة، ورفض 30 يومًا + 1 مللي ثانية، ورفض التاريخ المستقبلي والقيم التالفة، واستعادة الصوت والجزء والثانية، والحذف عند «إيقاف» أو اكتمال التسجيل. الحفظ محلي في `localStorage` على الجهاز والمتصفح نفسه، بلا حساب أو حفظ سحابي.

## الحفاظ على الإصلاحات السابقة

- الصفحة الرئيسية المختارة بالتصميم الكحلي–الفيروزي وبطاقات CTA وبطاقات «الأحدث» المدمجة.
- الأقسام الخمسة ملتفة على الجوال والتابلت بلا تمرير أفقي.
- مشاركة المسارات العربية بترميز واحد، وفتات مسار حقيقي، وفهرس H2 فقط.
- المقالات المرتبطة بحسب السلسلة/الوسوم، و`SearchAction` وPWA والأيقونات وCSP.
- منع CORS العام عن HTML، و404 بعلامة `noindex`، ومسار مشغّل الجهاز الاحتياطي للتابلت.
- منع `audio.load()` قبل اللمسة الأولى، والحفاظ على السرعات وشريط التقديم والإفصاح عن الصوت الاصطناعي.

## اختبار الحزمة النهائية

**PASS** — أُنشئ الأرشيف من مصدر بلا `node_modules` أو `dist` أو صوت عام مكرر، ثم:

1. فُك في مجلد مستقل.
2. نجح فحص `SOURCE-SHA256SUMS.txt` لجميع ملفات المصدر.
3. نجح `npm ci` من القفل المرفق.
4. أعيد استيراد Cedar + Hamed من `audio-releases/` بلا شبكة أو مفتاح.
5. أعيد بناء 50 صفحة وفحص 30 MP3 وجميع بوابات الإصدار بنجاح.
